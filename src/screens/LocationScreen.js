import React from "react";
import LocationTabView from '../components/LocationTabView'

const LocationScreen =  props => {

  return (
    <LocationTabView navigation={props.navigation}/>
  )
};

export default LocationScreen;
